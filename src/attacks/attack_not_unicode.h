#ifndef ATTACK_NOT_UNICODE_H
#define ATTACK_NOT_UNICODE_H

/**
 * Create a TAR archive with a non-Unicode name.
 * @param output_filename The name of the output file to generate.
 * @param index The index of the attack to perform.
 * @return void
 */
void attack_not_unicode(const char *output_filename, int index);

#endif /* ATTACK_NOT_UNICODE_H */
