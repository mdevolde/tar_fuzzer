#ifndef ATTACK_EXTRA_PADDING_H
#define ATTACK_EXTRA_PADDING_H

/**
 * Create a TAR archive with extra padding.
 * @param output_filename The name of the output file to generate.
 * @param index The index of the attack to perform.
 * @return void
 */
void attack_extra_padding(const char *output_filename, int index);

#endif /* ATTACK_EXTRA_PADDING_H */
