#ifndef ATTACK_PREFIX_INVALID_H
#define ATTACK_PREFIX_INVALID_H	

/**
 * Create a TAR archive with an invalid prefix.
 * @param output_filename The name of the output file to generate.
 * @param index The index of the attack to perform.
 * @return void
 */
void attack_prefix_invalid(const char *output_filename, int index);

#endif /* ATTACK_PREFIX_INVALID_H */
