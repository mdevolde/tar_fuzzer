#ifndef ATTACK_RANDOM_HEADER_H
#define ATTACK_RANDOM_HEADER_H

/**
 * Create a TAR archive with a random header.
 * @param output_filename The name of the output file to generate.
 * @param index The index of the attack to perform.
 * @return void
 */
void attack_random_header(const char *output_filename, int index);

#endif /* ATTACK_RANDOM_HEADER_H */
